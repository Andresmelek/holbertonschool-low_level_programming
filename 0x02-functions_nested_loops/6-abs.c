#include "holberton.h"
/**
 * main - check the code for Holberton School students.
 *
 * Return: Always 0.
 */
int _abs(int)
{

	int p;
	int n;
	
	n = p * -1;

	if (p >= 0)
		printf("%d", p);
	else
		printf("%d", n)
	return (0);
}
