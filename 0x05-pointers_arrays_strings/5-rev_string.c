#include "holberton.h"
/**
 * rev_string - function that reverses a string.
 * @s: parameter
 */
void rev_string(char *s)
{

	
	for (i = 0; s[i] != '\0'; i++)
	{
	return (s[i]);
	}
	{
		for (j = i;s[j] != '\0'; j--)
	return (s[j]);
	}
}
