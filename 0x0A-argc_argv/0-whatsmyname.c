/**
 * main - check the code for Holberton School students.
 * @argc: number of lines
 * @argv: value of the strings
 * Return: Always 0.
 */
int main(int argc, *char argv[])
{
	argc = argc;
	printf("%s\n", argv[0]);
	return (0);
}
